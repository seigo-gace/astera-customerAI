name: obsolete-notion-preflight-disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Notion transport disabled; Customer AI KB uses HF Private Storage only.'
